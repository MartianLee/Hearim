= form_for @today_letter do |f|
  - if @today_letter.errors.any?
    #error_explanation
      %h2= "#{pluralize(@letter.errors.count, "error")} prohibited this letter from being saved:"
      %ul
        - @letter.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :day
    = f.date_field :day
  .field
    = f.label :letter_id
    = f.number_field :letter_id
  .actions
    = f.submit 'Save'
